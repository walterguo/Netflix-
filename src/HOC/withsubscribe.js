import React, {Component} from 'react';

const withsubscribe = (wrappedComponent, data, type) => {
     return class extends Component {
        constructor(props) {
            super(props);
        }
        
     }
}